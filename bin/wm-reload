#!/usr/bin/env bash

for service in yabai spacebar skhd; do
  if pgrep -q "${service}"; then
    launchctl kickstart -k "gui/501/homebrew.mxcl.${service}"
  else
    brew services start "${service}"
  fi
done
